version: '3.8'

services:
  frontend:
    build: 
      context: ./frontend
    container_name: nlf-catalogo-frontend
    ports:
      - "5173:80"
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: nlf-catalogo-api
    env_file:
      - .env
    ports:
      - "5174:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

networks:
  default:
    name: nlf_network
